[phases.setup]
nixPkgs = ['nodejs-18_x']

[phases.install]
cmds = [
  'cd nahb-backend && npm ci',
  'cd nahb-frontend && npm ci'
]

[phases.build]
cmds = [
  'cd nahb-frontend && npm run build'
]

[start]
cmd = 'cd nahb-backend && npm start'
